type OAuthApplication
{
    id: Int!
    uuid: ID!
    code: String!
    secret: String!
    name: String!
    model: String!
}

type OAuthCredentials
{
    accessToken: String!
    refreshToken: String!
}